<%= form_for @order, url: send_sharing_email_retailer_order_path do |f| %>
  <%= f.email_field :customer_email, value: current_user.email %>
  <br><br>
  <%= f.submit value: 'Send Test Sharing Email' %>
  <br><br>
  <p><b>IMPORTANT: </b><i>Be carefull with other actions initiated from received email. For example, sharing an item will initiate a new email with a discount code (if present) - but email will be sent to the email address recorded with an order, not the one you put in the box above.</i></p>
<% end %>
